from enum import Enum


class FireMode(Enum):
    FULL_AUTO = {
        "name": "Full Auto",
        "short_name": "F"
    }
    SEMI_AUTO = {
        "name": "Semi Auto",
        "short_name": "S"
    }
